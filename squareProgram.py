#Function to find a perfect square and return it when less than the squares parameter
import math

def perfectSquare(n,p):
    if (math.sqrt(n))-int(math.sqrt(n)): #Check if input is a perfect square
       return ("Not a perfect square!")
    else:
       areaRoot = math.sqrt(n) 
       areaRoot <= p #Output perfect sqaure when less than paramaeter
       return (areaRoot)






